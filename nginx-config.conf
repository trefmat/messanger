# Nginx конфигурация для HTTPS reverse proxy
# Сохраните этот файл на сервер в /etc/nginx/sites-available/browsermessage.run.place
# Затем создайте символическую ссылку: ln -s /etc/nginx/sites-available/browsermessage.run.place /etc/nginx/sites-enabled/

# HTTP редирект на HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name browsermessage.run.place www.browsermessage.run.place;

    # Для certbot валидации
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Редирект всех других запросов на HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS сервер
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name browsermessage.run.place www.browsermessage.run.place;

    # SSL сертификаты Let's Encrypt
    ssl_certificate /etc/letsencrypt/live/browsermessage.run.place/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/browsermessage.run.place/privkey.pem;

    # SSL параметры для безопасности
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # HSTS (Strict Transport Security)
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

    # Безопасность
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Логирование
    access_log /var/log/nginx/browsermessage.run.place_access.log;
    error_log /var/log/nginx/browsermessage.run.place_error.log;

    # Ограничение размера загрузки
    client_max_body_size 10M;

    # Proxy к Node.js приложению
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # Заголовки для проксирования HTTP/1.1 и WebSocket
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # Отключаем буферирование для WebSocket
        proxy_buffering off;
    }
}
